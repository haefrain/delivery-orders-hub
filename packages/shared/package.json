{
  "name": "@delivery-hub/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain contracts: providers, order lifecycle, canonical order model",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
